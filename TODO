Move the debug function to the test file
Get the add command to register a project
 - Create a format
 - Register to a directory relative to the WD for now

 I need a day to clean up all the code
  - Get core functionallity out by then
  - Clear up
    - Duplicated logic
    - Inconsistent error handling
    - Inconsistencies in general
    - Weird or bad code
